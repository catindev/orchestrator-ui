apiVersion: v1
kind: Service
metadata:
  name: orchestrator-ui
  labels:
    app.kubernetes.io/name: orchestrator-ui
spec:
  selector:
    app.kubernetes.io/name: orchestrator-ui
  ports:
    - name: http
      port: 80
      targetPort: http
